name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Verify jq
        run: jq --version
      - name: Run resolver tests
        run: bash tests/run-tests.sh
